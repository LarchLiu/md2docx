---
format: html
theme: default
title: Custom HTML Title
standalone: true
diagramMode: live
baseTag: false
---

# Front Matter HTML Test

This document tests HTML-specific front matter options.

## Diagram Test

```mermaid
flowchart LR
    A[Start] --> B[Process]
    B --> C[End]
```

## Features Tested

- `format: html` - Output format
- `theme: default` - Theme selection
- `title: Custom HTML Title` - HTML document title
- `standalone: true` - Standalone HTML document
- `diagramMode: live` - Live diagram rendering
- `baseTag: false` - No base tag
